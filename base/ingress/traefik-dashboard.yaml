apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: traefik-dashboard
  namespace: kube-system
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - match: Host(`traefik.isaacwallace.dev`)
      kind: Rule
      middlewares:
        - name: default-headers
      services:
        - name: api@internal
          kind: TraefikService
  tls: {}