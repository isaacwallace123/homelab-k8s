apiVersion: secrets.doppler.com/v1alpha1
kind: DopplerSecret
metadata:
  name: grafana
  namespace: secrets
spec:
  tokenSecret:
    name: doppler-token-grafana
    key: serviceToken

  managedSecret:
    name: grafana-secrets
    namespace: monitoring
    type: Opaque